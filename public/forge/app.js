'use strict';
const $=id=>document.getElementById(id),app=$('app');
const previewOnly=window.FORGE_PREVIEW_ONLY===true;
const e=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let token=sessionStorage.getItem('forge.session')||'',catalog=[],local=false,live=false,operatorEmail='',draft=null,step=0,answers={},business={summary:'',website:null},email='',pendingKey=null,pollTimer;
let initial=true,questions=[],loginMode='create',identity=null;
const unknown="I'm not sure";
const labels={problem:'What work would you love to take off your plate?',audience:'Who does this work, and who needs the result?',data_access:'Which tools or information does it involve?',output:'What should your AI team help produce?',success:'What would a useful result look like?'};
async function api(body){const response=await fetch(window.FORGE_API||'/api',{method:'POST',headers:{'Content-Type':'application/json',...(token?{Authorization:'Bearer '+token}:{})},body:JSON.stringify(body)});const data=await response.json();if(!response.ok){if(response.status===401){token='';sessionStorage.removeItem('forge.session');}throw Error(data.error||'request_failed');}return data;}
const messages={pilot_recipient_rejected:'This live pilot is limited to Greg’s Workspace and personal Gmail addresses.',pilot_email_budget_exhausted:'The live pilot has reached its email allowance.',cooldown:'Please wait a minute before requesting another email.',version_conflict:'Your draft changed in another tab. Reloading the saved version.',work_in_progress:'Your design is already being prepared.',free_allowance_used:'Your free design allowance has been used. Resume your saved design.',invalid_code:'That code did not match. Please try again.',purpose_required:'Add a little more detail about the work you want help with.',unauthorized:'Sign in again to resume your saved design.',verification_unavailable:'Verification is temporarily unavailable. Your brief is still in this tab.',generation_disabled:'New designs are temporarily paused. Your progress is saved.',delivery_unavailable:'This email needs review. Your saved design is still available.',rate_limited:'Please wait before trying again.',invalid_text:'Please add a short answer, or choose “I’m not sure.”',batch_limit:'Your design has reached its final round.',credential_shaped_answer:'Please remove passwords, access tokens or other credentials.'};
function notice(error){if(error.message==='unauthorized'){loginMode=draft?'resume':loginMode;login();}$('message').textContent=messages[error.message]||'We could not finish that step. Your saved progress is safe. Try again shortly.';$('message').classList.add('visible');setTimeout(()=>$('message').classList.remove('visible'),6000);}
function focus(){app.focus();window.scrollTo({top:0,behavior:'instant'});}
function button(id,fn){$(id)?.addEventListener('click',async ev=>{const b=ev.currentTarget;b.disabled=true;try{await fn();}catch(err){notice(err);if(err.message==='version_conflict'&&draft){draft=await api({action:'read',draft_id:draft.draft_id});showDraft();}}finally{b.disabled=false;}});}
function stash(){if(initial)sessionStorage.setItem('forge.initial',JSON.stringify({business,answers,step}));}
function templateSample(){return `<aside class="sample" aria-labelledby="template-heading">
 <p class="eyebrow">EXAMPLE / PROJECT STRUCTURE</p><h2 id="template-heading">Expert agent template.</h2>
 <pre class="template-tree" aria-label="Simplified expert agent folder structure"><code><span class="tree-root">expert-agent/</span>
<span class="tree-branch" aria-hidden="true">├── </span>_expert.md
<span class="tree-branch" aria-hidden="true">├── </span>expertise.md
<span class="tree-branch" aria-hidden="true">├── </span>prime.md
<span class="tree-branch" aria-hidden="true">├── </span>question.md
<span class="tree-branch" aria-hidden="true">├── </span>plan.md
<span class="tree-branch" aria-hidden="true">├── </span>self-improve.md
<span class="tree-branch" aria-hidden="true">├── </span>justfile
<span class="tree-branch" aria-hidden="true">└── </span><span class="tree-root">profile/</span>
<span class="tree-branch" aria-hidden="true">    ├── </span>config.yaml
<span class="tree-branch" aria-hidden="true">    ├── </span>SOUL.md
<span class="tree-branch" aria-hidden="true">    └── </span>distribution.yaml</code></pre>
 <p class="hint">A simplified expert template: knowledge, working instructions, reusable commands, and its own runtime profile.</p>
</aside>`;}
function landing(){clearTimeout(pollTimer);app.innerHTML=`<div class="hero"><div><p class="eyebrow">YOUR BUSINESS. YOUR AI TEAM.</p><h1>What if the right team was already taking shape?</h1><p class="lead">Tell us what slows your business down. We’ll design an AI team around it, with clear roles, a practical workflow, and a plan for getting started.</p><button id="start" class="animated-cta"><span class="animated-cta-shimmer" aria-hidden="true"></span><span class="animated-cta-label">Design my AI team <span aria-hidden="true">↗</span></span></button><div class="meta"><span>A short business brief</span><span>Up to two focused follow-ups</span><span>Free design by email</span></div></div>${templateSample()}</div><section class="workspace-overview" id="forge-windows" aria-labelledby="forge-windows-heading"><p class="eyebrow">INSIDE THE WORKSPACE</p><h2 id="forge-windows-heading">A window for every part of the work.</h2><p class="workspace-intro">Explore the 13 windows in the Agent Forge workspace design. Your free intake creates a team design and configuration preview. Connecting tools and activating the workspace come afterward.</p><ul class="workspace-window-list"><li><strong>Skills Library</strong><span>The capabilities this expert can use.</span></li><li><strong>Justfile Commands</strong><span>Real recipes, inputs, and sample execution.</span></li><li><strong>Agent Presence</strong><span>The selected expert and its configured avatar.</span></li><li><strong>Conversation</strong><span>A focused conversation with the expert.</span></li><li><strong>Working Canvas</strong><span>The document or preview currently in focus.</span></li><li><strong>Proposals</strong><span>Review suggested work before it begins.</span></li><li><strong>Setup &amp; Quality Checks</strong><span>Configuration completeness and verification evidence.</span></li><li><strong>Expert Configuration</strong><span>Purpose, model, operating limits, and instructions.</span></li><li><strong>Second Brain</strong><span>Context, saved decisions, and source documents.</span></li><li><strong>Tasks &amp; Workstream</strong><span>An expert-scoped view of work and dependencies.</span></li><li><strong>Artifacts</strong><span>Find outputs and the sources behind them.</span></li><li><strong>Run Console</strong><span>Follow command output and execution history.</span></li><li><strong>Changes &amp; Approvals</strong><span>Compare and review the exact proposed change.</span></li></ul></section>`;button('start',()=>{initial=true;questions=catalog;step=0;wizard();});focus();}
function preview(){
 landing();
 $('resume').hidden=true;
 $('operator-link').hidden=true;
 $('preview-banner').textContent='Agent Forge preview. Automated team design and email delivery are coming soon.';
 $('preview-banner').hidden=false;
 app.querySelector('h1').textContent='A workspace built around your AI team.';
 app.querySelector('.lead').textContent='Explore how Agent Forge brings expert roles, shared context, and practical workflows together. For a team designed around your business, start a conversation with Greg.';
 $('start').outerHTML='<a class="animated-cta" href="/#contact"><span class="animated-cta-shimmer" aria-hidden="true"></span><span class="animated-cta-label">Talk about your AI team <span aria-hidden="true">↗</span></span></a>';
 app.querySelector('.meta').innerHTML='<span>Expert roles</span><span>Connected workflows</span><span>Configuration preview</span>';
 app.querySelector('.workspace-intro').textContent='Explore the 13 windows in the Agent Forge workspace design, from expert configuration to reviewing the work your team produces.';
 $('retention-note').textContent='Workspace preview · No intake data collected';
}
function wizard(){clearTimeout(pollTimer);const total=questions.length+(initial?1:0),q=initial&&step===0?null:questions[step-(initial?1:0)];const value=q?answers[q.question_id]||'':business.summary;const round=draft?Math.min(3,draft.usage.submitted_batches+1):1;
 app.innerHTML=`<section class="wizard"><div class="step-label"><span>Round ${round} of up to 3</span><span>Question ${step+1} of ${total}</span></div><div class="progress"><span style="width:${100*(step+1)/total}%"></span></div><p class="eyebrow">${q?'SHAPE YOUR TEAM':'A LITTLE CONTEXT'}</p><h1>${q?e(labels[q.question_id]||q.label):'Tell us a little about your business.'}</h1><p>${q?e(q.reason):'What do you do, and who do you help? A few sentences is plenty.'}</p><label for="answer">${q?'Your answer':'Business description'}</label><textarea id="answer" maxlength="2000" placeholder="A short description is enough…">${e(value)}</textarea>${!q?`<label for="website">Company website <span class="hint">(optional)</span></label><input id="website" type="url" value="${e(business.website)}" placeholder="https://example.com">`:`<div class="choices">${q.choices.map((x,i)=>`<button class="choice" data-choice="${i}">${e(x.length>75?x.slice(0,72)+'…':x)}</button>`).join('')}<button id="unknown" class="choice">I’m not sure</button></div>`}<p class="hint">Describe your work and tools. Please leave out passwords, access tokens and sensitive customer records.</p><div class="actions"><button id="back" class="quiet">Back</button><button id="next">${step===total-1?'Review answers':'Continue'} →</button></div></section>`;
 const capture=()=>{if(q)answers[q.question_id]=$('answer').value.trim();else{business.summary=$('answer').value.trim();business.website=$('website').value.trim()||null;}stash();};
 $('answer').addEventListener('input',capture);$('website')?.addEventListener('input',capture);
 app.querySelectorAll('[data-choice]').forEach(b=>b.onclick=()=>{$('answer').value=q.choices[Number(b.dataset.choice)];capture();});button('unknown',()=>{$('answer').value=unknown;capture();});
 button('back',()=>{capture();if(step>0){step--;wizard();}else if(!initial)showDraft();else landing();});
 button('next',()=>{capture();if(!$('answer').value.trim())throw Error('invalid_text');if(!q&&business.website){const u=new URL(business.website);if(!['https:','http:'].includes(u.protocol))throw Error('invalid_text');}step++;stash();if(step===total)review();else wizard();});focus();}
function review(){app.innerHTML=`<section class="wizard"><p class="eyebrow">A QUICK LOOK BEFORE WE CONTINUE</p><h1>Your team starts here.</h1>${initial?`<div class="review-row"><small>Business context</small>${e(business.summary)}</div>`:''}${questions.map((q,i)=>`<div class="review-row"><button class="quiet" data-edit="${i+(initial?1:0)}">Edit</button><small>${e(labels[q.question_id]||q.label)}</small>${e(answers[q.question_id])}</div>`).join('')}<p class="hint">We’ll ask only a few more questions if they help define your design. “I’m not sure” stays an open question for onboarding.</p><div class="actions"><button id="review-back" class="quiet">Back</button><button id="submit">${initial?'Save and receive my design':'Submit these answers'} →</button></div></section>`;
 app.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{step=Number(b.dataset.edit);wizard();});button('review-back',()=>{step=questions.length+(initial?1:0)-1;wizard();});button('submit',async()=>{if(initial){if(!token){loginMode='create';login();}else await createAndSubmit();}else{draft=await mutate('save',{answers});await submitBatch();}});focus();}
function login(){clearTimeout(pollTimer);app.innerHTML=`<section class="login"><p class="eyebrow">${loginMode==='operator'?'PRIVATE WORKBENCH':'SAVE YOUR DESIGN'}</p><h1>${loginMode==='create'?'Where should we send your team design?':'Welcome back.'}</h1><p>${loginMode==='create'?'We’ll verify your email before preparing the design. You can use it to return to your saved work.':'Verify your email to continue with your saved design.'}</p><form id="email-form"><label for="email">Email address</label><input id="email" type="email" autocomplete="email" value="${e(loginMode==='operator'?(live?operatorEmail:'operator@example.test'):email)}" required><button id="send-code" style="margin-top:20px">Send verification code</button></form><div id="code-area"></div><p class="hint">We’ll use your email for this design and its saved link. This does not subscribe you to marketing.</p>${local&&!live?'<p class="deferred">This local preview sends no email. It will show a test code here.</p>':''}</section>`;
 $('email-form').onsubmit=async ev=>{ev.preventDefault();const b=$('send-code');b.disabled=true;try{email=$('email').value.trim();const r=await api({action:'request_verification',email});$('code-area').innerHTML=`<form id="code-form"><label for="code">Verification code</label><input id="code" inputmode="numeric" autocomplete="one-time-code" maxlength="10" required>${r.local_code?`<p class="hint">Local test code: <strong>${e(r.local_code)}</strong></p>`:'<p class="hint">Check your inbox for the code.</p>'}<button id="verify" style="margin-top:16px">Verify and continue</button></form>`;$('code').focus();$('code-form').onsubmit=verify;}catch(err){notice(err);}finally{b.disabled=false;}};focus();}
async function verify(ev){ev.preventDefault();$('verify').disabled=true;try{const r=await api({action:'verify_code',email,code:$('code').value});token=r.access_token;sessionStorage.setItem('forge.session',token);identity=await api({action:'session'});if(loginMode==='create')await createAndSubmit();else if(loginMode==='operator'){const target=sessionStorage.getItem('forge.workbench-target');location.href=target||'/operator-status/';}else await resume();}catch(err){notice(err);if($('verify'))$('verify').disabled=false;}}
async function mutate(action,extra={}){const body={action,draft_id:draft.draft_id,version:draft.draft_version,...extra};const fingerprint=JSON.stringify(body);if(!pendingKey||pendingKey.fingerprint!==fingerprint)pendingKey={fingerprint,key:crypto.randomUUID()};const result=await api({...body,key:pendingKey.key});pendingKey=null;return result;}
async function createAndSubmit(){draft=await api({action:'create',business,answers});sessionStorage.removeItem('forge.initial');if(draft.stage==='collecting')await submitBatch();else showDraft();}
async function submitBatch(){draft=await mutate('submit');showDraft();}
async function newPilot(){await api({action:'pilot_new_design'});draft=null;initial=true;answers={};business={summary:'',website:null};history.replaceState({},'', '/forge/');landing();}
function showDraft(){if(draft.stage==='collecting'){initial=true;questions=catalog;business=draft.business;answers=Object.fromEntries(draft.revisions.flatMap(r=>r.answers.map(a=>[a.question_id,a.value])));review();return;}if(['queued','generating'].includes(draft.stage)){working();return;}if(draft.stage==='needs_answers'){initial=false;questions=draft.issued_question_set.questions;answers=Object.fromEntries(draft.revisions.filter(r=>r.question_set_version===draft.issued_question_set.version).flatMap(r=>r.answers.map(a=>[a.question_id,a.value])));step=0;wizard();return;}if(draft.artifact){result().catch(notice);return;}app.innerHTML=`<section class="working"><p class="eyebrow">YOUR PROGRESS IS SAVED</p><h1>This design needs another look.</h1><p>We couldn’t produce a valid design within the current attempt. Your answers are saved, and no tools have been connected.</p>${draft.usage.shared_retries<1&&draft.usage.model_attempts<4?'<button id="retry">Try the saved brief once more</button>':'<a class="button" href="https://calendar.app.google/X4SN26PYLgvVYPRp8">Book a design review</a>'}${live?'<button id="new-pilot" class="quiet">Start another pilot design</button>':''}</section>`;button('new-pilot',newPilot);button('retry',async()=>{draft=await mutate('retry');showDraft();});focus();}
function working(){app.innerHTML=`<section class="working"><div class="orb" aria-hidden="true"></div><p class="eyebrow">YOUR BRIEF IS TAKING SHAPE</p><h1>Finding the right next step.</h1><p>We’re working from your saved answers. You can leave and resume with the same email.</p><p class="hint">Round ${draft.usage.submitted_batches} of up to 3</p></section>`;focus();pollTimer=setTimeout(poll,700);}
async function poll(){try{draft=await api({action:'read',draft_id:draft.draft_id});if(['queued','generating'].includes(draft.stage))pollTimer=setTimeout(poll,700);else showDraft();}catch(err){notice(err);}}
async function result(){clearTimeout(pollTimer);const artifact=await api({action:'artifact',draft_id:draft.draft_id});const state=draft.delivery.state;app.innerHTML=`<div class="result-header"><div><p class="eyebrow">YOUR SAVED DESIGN</p><h1>Meet your proposed AI team.</h1></div><span class="status-pill">${state==='delivered'?'Email delivered':state==='accepted'?(local&&!live?'Preview email accepted':'Email accepted for delivery'):['failed','bounced','complained'].includes(state)?'Email needs review':'Email queued'}</span></div><p>Your design is ready here, even if email takes a little longer. It includes the questions to resolve before building.</p><div class="result-actions">${live?'<button id="new-pilot" class="quiet">Start another pilot design</button>':''}<button id="download" class="quiet">Download design</button><button id="config-download" class="quiet">Download config preview</button><button id="resend-result" class="quiet">Resend saved link</button>${local?'<button id="workbench" class="quiet">Review private workbench handoff</button>':''}</div><iframe class="result-frame" title="Your proposed team design" sandbox="allow-popups allow-popups-to-escape-sandbox" id="design" style="margin-top:24px"></iframe><p class="hint">Draft configuration requires onboarding before it can become a running agent.</p><button id="delete" class="quiet">Delete my saved design</button>`;$('design').srcdoc=artifact.html;
 const download=(data,name,type)=>{const url=URL.createObjectURL(new Blob([data],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
 button('new-pilot',newPilot);button('download',()=>download(artifact.html,'my-ai-team.html','text/html'));button('config-download',()=>download(JSON.stringify(artifact.config_preview,null,2),'team-config-preview.json','application/json'));
 button('resend-result',async()=>{draft=await mutate('resend');await result();});button('workbench',()=>{const target='/workbench/?draft='+encodeURIComponent(draft.draft_id)+'&hash='+encodeURIComponent(draft.artifact.content_sha256);sessionStorage.setItem('forge.workbench-target',target);if(identity?.operator)location.href=target;else{loginMode='operator';login();}});
 button('delete',async()=>{if(!confirm('Delete this saved design and its answers? Your free-design allowance remains until its 30-day window expires.'))return;await api({action:'delete',draft_id:draft.draft_id});draft=null;landing();});focus();}
async function resume(){if(!token){loginMode='resume';login();return;}identity=await api({action:'session'});const drafts=await api({action:'list'});const id=location.pathname.startsWith('/forge/designs/')?location.pathname.split('/').pop():null;draft=id?drafts.find(d=>d.draft_id===id):drafts[0];if(draft)showDraft();else{notice(Error('not_found'));landing();}}
$('resume').onclick=()=>{loginMode='resume';resume().catch(err=>{notice(err);login();});};
(async()=>{if(previewOnly){preview();return;}try{const data=await api({action:'catalog'});catalog=data.questions;local=data.local_preview;live=!!data.live_pilot;operatorEmail=data.operator_email||'';if(live)$('preview-banner').textContent='Live pilot: real Gmail and OpenRouter. Limited to your test accounts.';$('preview-banner').hidden=!local;$('operator-link').hidden=!local;try{const saved=JSON.parse(sessionStorage.getItem('forge.initial'));if(saved){business=saved.business;answers=saved.answers;}}catch{}if(location.pathname==='/operator'){loginMode='operator';login();}else if(location.pathname.startsWith('/forge/designs/'))await resume();else landing();}catch(err){notice(err);app.innerHTML='<h1>The workshop is temporarily unavailable.</h1><p>Please try again shortly.</p>';}})();
