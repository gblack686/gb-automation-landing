import { ClientWorkspaceRoutes } from '../workspace/ClientWorkspaceRoutes';

const SLUG = 'gbautomation';

export default function GbautomationPortal() {
  return <ClientWorkspaceRoutes slug={SLUG} />;
}
